 % python3 a_maze_ing.py sample_config.txt
Traceback (most recent call last):
  File "/home/brisnovs/projects/amazing/mouj/a_maze_ing.py", line 30, in <module>
    main()
    ~~~~^^
  File "/home/brisnovs/projects/amazing/mouj/a_maze_ing.py", line 16, in main
    graph = Graph(maze_specs)
  File "/home/brisnovs/projects/amazing/mouj/src/graph.py", line 14, in __init__
    self.cells: list[Cell] = self._build_cells(self.blueprint)
                             ~~~~~~~~~~~~~~~~~^^^^^^^^^^^^^^^^
  File "/home/brisnovs/projects/amazing/mouj/src/graph.py", line 24, in _build_cells
    cell.is_active = False
    ^^^^^^^^^^^^^^
  File "<string>", line 17, in __setattr__
dataclasses.FrozenInstanceError: cannot assign to field 'is_active'


The dataclass Cell is frozen (immutable), so assigning to cell.is_active raises FrozenInstanceError. Fix by constructing Cell with the desired is_active value (if the dataclass has that field), or by making Cell mutable.

Two straightforward fixes:

Preferable — respect immutability: set is_active at construction
If Cell signature is e.g. Cell(x: int, y: int, is_active: bool = True), change creation to: cell = Cell(x, y, is_active=(blueprint[y, x] != False))
And likewise when creating neighbour_cell in _build_walls use Cell(nx, ny, is_active=(blueprint[ny, nx] != False)) or better: look up the cell from self.cells (see note below).
Make Cell mutable (less recommended)
Remove frozen=True from the @dataclass decorator in maze.structs so assignment is allowed.
Extra improvements (recommended):

In _build_walls, don’t create new Cell objects for neighbours (that makes multiple distinct Cell instances for same coords). Instead index into self.cells (or keep a 2D array mapping coords to Cell) so walls connect the same Cell objects:
build a 2D map first
cell_map = {(c.x, c.y): c for c in self.cells} ... neighbour = cell_map[(nx, ny)]
Pick option 1 + the cell_map change for correct behavior and to avoid duplicate Cell instances.


Replace your _build_cells and _build_walls methods with this code (copy-paste into your Graph class):



def _build\_cells(self, blueprint: np.ndarray) -> list[Cell]:
    cells = []
    for y in range(self.height):
        for x in range(self.width):
            is\_active = bool(blueprint[y, x])
            cells.append(Cell(x, y, is\_active=is\_active))
    return cells

def _build\_walls(self) -> list[Wall]:
    direction = [(1, 0), (0, 1)]
    walls = []
    # map coordinates to the single Cell instances created above
    cell\_map = {(c.x, c.y): c for c in self.cells}

    for cell in self.cells:
        if not cell.is\_active:
            continue
        for dx, dy in direction:
            nx, ny = cell.x + dx, cell.y + dy
            if self._inside\_check(nx, ny):
                neighbour\_cell = cell\_map[(nx, ny)]
                if neighbour\_cell.is\_active:
                    walls.append(Wall(cell, neighbour\_cell))
    return walls


    _______________________________________________________________

    Traceback (most recent call last):
  File "/home/brisnovs/projects/amazing/mouj/a_maze_ing.py", line 30, in <module>
    main()
    ~~~~^^
  File "/home/brisnovs/projects/amazing/mouj/a_maze_ing.py", line 25, in main
    exporter.write_to_file()
    ~~~~~~~~~~~~~~~~~~~~~~^^
  File "/home/brisnovs/projects/amazing/mouj/maze/exporter.py", line 60, in write_to_file
    grid_str: list[str] = self._build_grid()
                          ~~~~~~~~~~~~~~~~^^
  File "/home/brisnovs/projects/amazing/mouj/maze/exporter.py", line 47, in _build_grid
    bitmask: dict[C, int] = self._build_bitmask()
                            ~~~~~~~~~~~~~~~~~~~^^
  File "/home/brisnovs/projects/amazing/mouj/maze/exporter.py", line 23, in _build_bitmask
    c_bits: dict[C, int] = {c: 0 for c in c_items}
                                          ^^^^^^^
NameError: name 'c_items' is not defined


Define the type variable C (and any other missing names) before using it in annotations. Add near the top:

python


from typing import TypeVar, Dict, Tuple, List

C = TypeVar("C")  # represents Cell-like keys in bitmask dict
Then update annotations and undefined names used in the methods:

Replace dict[C, int] with Dict[C, int] (or keep bracket form if using Python 3.9+).
Ensure c_items is defined — it should be the iterable of cells; use self.all_cells instead:
c_bits: Dict[C, int] = {c: 0 for c in self.all_cells}
In _build_grid use the same variable name bitmask (you used c_bits incorrectly):
max_x = max(c.x for c in bitmask)
max_y = max(c.y for c in bitmask)
Or compute from self.all_cells: max(c.x for c in self.all_cells)
Fix write_to_file issues:

_build_path_str returns list[str]; convert to string: path_str = ''.join(self._build_path_str())
entry/extry are tuples of ints — convert to strings: entry = ','.join(map(str, self.ent))
Open file for writing: with open(self.output_file, 'w', encoding='utf-8') as f:
Except clause: catch specific exceptions or Exception only: except Exception as e:
Example corrected snippets:

python


from typing import TypeVar, Dict, Tuple, List

C = TypeVar("C")
# ...

def _build_bitmask(self) -> Dict[C, int]:
    c_bits: Dict[C, int] = {c: 0 for c in self.all_cells}
    ...
    return c_bits

def _build_grid(self) -> List[str]:
    bitmask = self._build_bitmask()
    lookup_dict = {(c.x, c.y): bits for c, bits in bitmask.items()}
    max_x = max(c.x for c in self.all_cells)
    max_y = max(c.y for c in self.all_cells)
    ...
Apply those fixes and rerun; if you still get NameError(s), import or define any remaining missing symbols (c_items, c_bits) as above._

what is the minimal fix? 

Minimal fix (3 tiny edits):

Define C and use self.all_cells instead of c_items — add at top:
python


from typing import TypeVar
C = TypeVar("C")
In _build_bitmask replace c_items with self.all_cells:
python


c_bits: dict[C, int] = {c: 0 for c in self.all_cells}
In _build_grid use bitmask (not c_bits) for max calculations:
python


max_x: int = max(c.x for c in bitmask)
max_y: int = max(c.y for c in bitmask)
Those three changes remove the NameError with minimal edits.

why are fix 2 and 3 required?  Would 1 suffice? 

No — 1 alone won't fix the runtime NameError(s).

Why:

Fix 1 (C = TypeVar("C")) only defines the type variable used in annotations. That prevents the NameError for C in annotations.
But your code also references c_items and c_bits at runtime:
c_items is used when building c_bits; it wasn't defined anywhere, so the interpreter will raise NameError at that line regardless of the type variable.
c_bits is referenced in _build_grid when computing max_x/max_y, but you actually created a local variable named bitmask; using c_bits will raise NameError.
Therefore you must either define c_items and c_bits appropriately or change those references to the variables you do have (self.all_cells and bitmask). Fixes 2 and 3 replace undefined names with the correct ones and are required in addition to defining C._


_______________________________________________________________
PATH FINDER:
The problem is that you construct fresh Cell instances for entry/exit (Cell(*entry_point)) which are distinct objects from the Cell instances used as keys in adj_map and came_from, so equality/finds fail unless dataclass equality matches by value — yours is frozen dataclass so equality by value works, but you're using sets/dicts keyed by the original Cell objects; creating new Cell objects is fine for equality but not for identity in your adjacency map because the adjacency map's keys are the original Cell instances and you later index adj_map[current] where current is the newly created entry_cell — that will KeyError unless the dataclass equality/hash match. However, dataclasses default hashing when frozen uses object identity unless hash generated; with frozen=True dataclass is hashable by field values, so it should work. Still the symptom (only one cell) indicates the BFS never reached exit — likely because adj_map uses different Cell objects (from graph.cells) while you created entry_cell/exit_cell not looked up from graph.

Minimal robust fix: look up the actual Cell objects from graph.cells by coordinates and use those as entry/exit (so keys match exactly):

Replace in solve_maze:

python


entry_cell = next(c for c in self.all_cells if (c.x, c.y) == entry_point)
exit_cell  = next(c for c in self.all_cells if (c.x, c.y) == exit_point)
(or use a lookup dict built once). That ensures you're using the same Cell instances as in adj_map / came_from*

Also add a safety check to handle missing points:

python


try:
    entry_cell = next(...)
except StopIteration:
    raise ValueError("Entry point not found")
With that change BFS will traverse correctly and return the full path.*



PATH IS EMPTY
Almost — indentation error and you reuse q for the main BFS so it’s empty when you get there. Fixes:

Fix indentation inside the connectivity loop (two lines were misindented).
Use a temporary queue for the connectivity test so you can run the real BFS afterwards.
Minimal corrected version (only the solve_maze body shown):

python


entry_cell = self.cell_lookup.get(entry_point)
exit_cell  = self.cell_lookup.get(exit_point)
if entry_cell is None or exit_cell is None:
    raise ValueError("Entry or exit point not found in graph.cells")

# connectivity test using a temporary queue
temp_q = deque([entry_cell])
seen = {entry_cell}
reachable = set()
while temp_q:
    cur = temp_q.popleft()
    reachable.add(cur)
    for n in self.adj_map[cur]:
        if n not in seen:
            seen.add(n)
            temp_q.append(n)
print("reachable count:", len(reachable))
print("exit reachable?", exit_cell in reachable)

# real BFS
visited = {entry_cell}
came_from = {entry_cell: None}
q = deque([entry_cell])
while q:
    current = q.popleft()
    if current == exit_cell:
        break
    for neighbour in self.adj_map[current]:
        if neighbour not in visited:
            visited.add(neighbour)
            came_from[neighbour] = current
            q.append(neighbour)

if exit_cell not in came_from:
    return []

solution = []
crumbs = exit_cell
while crumbs is not None:
    solution.append(crumbs)
    crumbs = came_from.get(crumbs)
solution.reverse()
print(solution)
return solution
Run that and paste the printed "reachable count" and "exit reachable?" results if it still returns an empty solution.

____
 % python3 a_maze_ing.py sample_config.txt
reachable count: 1
exit reachable? False
0

Reachable count 1 means entry_cell has no neighbors in adj_map — your adjacency is empty for that cell. Likely causes & quick checks:

Maze vs walls meaning
You build open_walls = set(self.all_walls) - set(self.maze). Verify what self.maze contains when you construct BFS: it must be the list of removed/OPEN walls (passages). If self.maze instead contains open walls, then open_walls becomes empty (or vice versa). Print lengths where BFS is created:
print(len(self.all_walls), len(self.maze))
Verify wall endpoints match cells
Print an example open_walls element and its cells:
print(next(iter(open_walls))) before returning adj_map
print(list(open_walls)[:5]) and their (cell_a.x,cell_a.y),(cell_b.x,cell_b.y)
Confirm cells used as keys are same instances as in walls
Print entry_cell and any wall that references that cell:
for w in self.all_walls: if (w.cell_a.x,w.cell_a.y)==(entry_cell.x,entry_cell.y) or (w.cell_b...): print("wall references entry:", w) break
Quick actionable changes to diagnose:
In _build_adj add:
print("all_walls:", len(self.all_walls), "maze param:", len(self.maze))
print("sample wall:", self.all_walls[0])
print("open_walls count:", len(open_walls))
Most likely fix (if you passed wrong set to BFS): when creating BFS you probably passed the wrong variable — ensure you call BFS(graph, maze) where maze is the list of removed walls (pass the same value your exporter used). If generators/other code store walls in attributes with different meanings, change open_walls line to whichever produces adjacency of passages:

If self.maze is the set of removed/open walls already, use: open_walls = set(self.maze)
If self.maze is the set of remaining/closed walls, use:
open_walls = set(self.all_walls) - set(self.maze_
Adjust accordingly and rerun; paste the prints if you want me to pinpoint the mismatch._